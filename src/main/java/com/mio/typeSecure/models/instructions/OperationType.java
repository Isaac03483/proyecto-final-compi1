package com.mio.typeSecure.models.instructions;

public enum OperationType {

    PLUS, MINUS, TIMES, DIVIDE,MOD, EQUALS, NOT_EQUALS, LESS, GREATER, LESS_EQ, GREATER_EQ, AND, OR,
    NOT, INCREMENT, DECREMENT
}
